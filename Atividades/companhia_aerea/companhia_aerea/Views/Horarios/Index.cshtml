@model IEnumerable<companhia_aerea.Models.Horario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InicioHorario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinalHorario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdVooNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InicioHorario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FinalHorario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdVooNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
